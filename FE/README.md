# FE

`exercise-app/` là giao diện VUDN và dữ liệu 16 module. Giao diện được Express phục vụ tại `/learning_code/exercise-app/index.html` để các đường dẫn lời giải trong module vẫn hoạt động.

Chạy toàn bộ ứng dụng từ thư mục gốc bằng:

```powershell
docker compose up --build -d
```
