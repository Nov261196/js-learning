let user = {
    name: "MK",
    age: 18,
    city: "HCM"
};

for(let name = 0; i < user.length; i++){
    console.log(name[i]);
}

console.log(user([name]));
console.log(user([age]));
console.log(user([city]));