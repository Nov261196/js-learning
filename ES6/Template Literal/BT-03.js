const product = {
    name: "Laptop",
    price: 1500,
    quantity: 3,
};

console.log(`Sản Phẩm: ${product.name}`);
console.log(`Giá: ${product.price}`);
console.log(`Số lượng: ${product.quantity}`);
console.log(`Tổng tiền: ${product.price * product.quantity}`)

