| Module                          | Nội dung                                                                                                                      | Mức cần đạt                  |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| **01. JavaScript Fundamentals** | `let`, `const`, data types, operators, `if/else`, `switch`, loops                                                             | Phải chắc                    |
| **02. Functions**               | function declaration, expression, parameters, arguments, `return`, scope                                                      | Phải chắc                    |
| **03. Array & Object**          | array/object, CRUD dữ liệu, loop qua dữ liệu                                                                                  | Phải chắc                    |
| **04. ES6+**                    | template literal, arrow function, destructuring, rest, spread, enhanced object literal, optional chaining, nullish coalescing | Phải chắc                    |
| **05. Array Methods**           | `forEach`, `map`, `filter`, `find`, `findIndex`, `some`, `every`, `reduce`, `sort`                                            | **Rất quan trọng cho React** |
| **06. Function nâng cao**       | callback, higher-order function, lexical scope, closure cơ bản                                                                | Quan trọng                   |
| **07. Modules**                 | `export`, `import`, default/named export                                                                                      | **Rất quan trọng cho React** |
| **08. DOM**                     | select element, sửa content/style/class/attribute, create/remove element                                                      | Cần hiểu                     |
| **09. Events & Forms**          | click, input, change, submit, event object, `preventDefault()`                                                                | Cần hiểu                     |
| **10. Async JavaScript**        | synchronous/asynchronous, event loop cơ bản, `setTimeout`                                                                     | Quan trọng                   |
| **11. Promise**                 | Promise states, `.then()`, `.catch()`, `.finally()`                                                                           | Quan trọng                   |
| **12. Async/Await**             | `async`, `await`, `try/catch`, error handling                                                                                 | **Phải chắc**                |
| **13. API & Fetch**             | HTTP cơ bản, JSON, `fetch`, GET/POST/PUT/PATCH/DELETE                                                                         | **Phải chắc**                |
| **14. Browser Storage**         | `localStorage`, JSON stringify/parse                                                                                          | Nên biết                     |
| **15. JS thực chiến**           | Todo, Product Filter, Weather/API app                                                                                         | Phải tự làm được             |
| **16. Chuẩn bị React**          | immutable data, state mindset, component mindset, npm/Vite cơ bản                                                             | Chuyển React                 |
